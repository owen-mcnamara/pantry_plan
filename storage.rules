rules_version = '2';
service firebase.storage {
  match /b/{bucket}/o {
    // Default deny-all until your app needs Storage uploads.
    match /{allPaths=**} {
      allow read, write: if false;
    }
  }
}